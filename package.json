{
  "name": "GoogleSpreadsheetOsuUtils",
  "version": "1.0.0",
  "scripts": {
    "lint": "gts lint",
    "clean": "gts clean",
    "fix": "gts fix",
    "add": "node add.mjs",
    "compile": "tsc && cp src/appsscript.json build/src/appsscript.json",
    "prepare": "pnpm run compile",
    "pretest": "pnpm run compile",
    "posttest": "pnpm run lint"
  },
  "devDependencies": {
    "@google/clasp": "3.0.3-alpha",
    "@types/google-apps-script": "^1.0.97",
    "@types/node": "^22.7.5",
    "gts": "^6.0.2",
    "import": "^0.0.6",
    "multi-clasp2": "^3.0.21",
    "typescript": "^5.1.6"
  }
}
